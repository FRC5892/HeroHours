{
  "name": "HeroHours Development",
  "dockerFile": "Dockerfile",
  "context": "..",
  
  "forwardPorts": [8000],
  "portsAttributes": {
    "8000": {
      "label": "HeroHours Server",
      "onAutoForward": "notify"
    }
  },

  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance",
        "batisteo.vscode-django",
        "wholroyd.jinja",
        "ms-python.black-formatter"
      ],
      "settings": {
        "python.defaultInterpreterPath": "/usr/bin/python3",
        "python.linting.enabled": true,
        "python.linting.pylintEnabled": false,
        "python.formatting.provider": "black",
        "terminal.integrated.defaultProfile.linux": "bash"
      }
    }
  },

  "postCreateCommand": "pip3 install --no-cache-dir -r requirements.txt && python3 manage.py migrate --noinput",
  "postStartCommand": "echo '🚀 HeroHours is ready! Run: python3 manage.py runserver'",

  "remoteUser": "vscode",
  
  "features": {
    "ghcr.io/devcontainers/features/git:1": {}
  }
}